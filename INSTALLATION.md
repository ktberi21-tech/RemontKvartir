# Инструкция по установке и развертыванию

Полное руководство по настройке, локальной разработке и развертыванию лендинга.

## 📋 Требования

- **Node.js** 14+ ([скачать](https://nodejs.org/))
- **Git** ([скачать](https://git-scm.com/))
- **Современный браузер** (Chrome, Firefox, Safari, Edge)
- **Текстовый редактор** (VS Code рекомендуется)

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Клонирование репозитория

```bash
# Откройте терминал и выполните:
git clone https://github.com/yourusername/remont-landing.git
cd remont-landing
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

Это установит `http-server` для локального тестирования.

### Шаг 3: Запуск локального сервера

```bash
npm run dev
```

Браузер автоматически откроется на `http://localhost:3000`

### Шаг 4: Готово! 🎉

Отредактируйте HTML-файлы и видите изменения в реальном времени.

---

## 📝 Конфигурация

### 1. Обновите информацию о компании

Откройте `CONFIG.md` и замените:

```javascript
company: {
    name: "Ваша компания",
    phone: "+7 (XXX) XXX-XX-XX",
    email: "ваш@email.ru",
    telegram: "@ваш_telegram_username",
    whatsapp: "79991234567",
    address: "Ваш адрес"
}
```

### 2. Обновите HTML содержание

Откройте `index.html` и найдите/замените:

```html
<!-- Главный заголовок -->
<h1 class="hero__title">Ремонт квартир под ключ с гарантией качества</h1>

<!-- Контакты в footer -->
<a href="mailto:info@yoursite.ru">info@yoursite.ru</a>
<a href="tel:+79991234567">+7 (999) 123-45-67</a>
```

### 3. Добавьте изображения

Создайте папки в `images/`:

```
images/
├── hero/
│   └── main.jpg (1920x1080px, 200-300KB)
├── portfolio/
│   ├── 1.jpg (280x250px)
│   ├── 2.jpg
│   └── ... (20+ фото)
├── team/
│   ├── 1.jpg (400x500px)
│   ├── 2.jpg
│   └── ... (4-6 фото)
├── testimonials/
│   ├── 1.jpg (50x50px)
│   └── ... (6-8 фото)
└── logo.svg
```

### 4. Настройте аналитику

#### Google Analytics

1. Перейдите на https://analytics.google.com
2. Создайте новый проект
3. Получите код (выглядит как `G-XXXXXXXXXX`)
4. В `index.html` найдите и замените:

```html
<!-- Строка 1 -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>

<!-- Строка 2 -->
gtag('config', 'G-XXXXXXXXXX');
```

#### Яндекс.Метрика

1. Перейдите на https://metrica.yandex.ru
2. Создайте новый счётчик
3. Получите ID (только цифры, например: 1234567890)
4. В `index.html` найдите и замените:

```javascript
ym(1234567890, "init", {...});
```

### 5. Настройте Telegram

В `main.js` обновите:

```javascript
const CONFIG = {
    telegram: {
        link: "https://t.me/ваш_username",
        startMessage: "Привет! Я заинтересован в ремонте квартиры"
    }
};
```

---

## 🎨 Кастомизация

### Изменение цветов

Откройте `css/style.css` и отредактируйте CSS переменные в `:root {}`:

```css
:root {
    --primary-color: #2180CD;      /* Основной цвет (кнопки, заголовки) */
    --secondary-color: #5E5240;    /* Вторичный цвет */
    --accent-color: #E67D60;       /* Акцентный цвет */
    --text-color: #134252;         /* Цвет текста */
    --bg-color: #FCFCF9;           /* Фоновый цвет */
}
```

### Изменение шрифтов

В `css/style.css` найдите:

```css
--font-main: 'Inter', sans-serif;     /* Основной шрифт */
--font-heading: 'Roboto', sans-serif; /* Шрифт заголовков */
```

Или в `index.html` в `<head>` отредактируйте ссылку Google Fonts.

### Добавление новых разделов

1. Добавьте HTML в `index.html` перед `</body>`
2. Создайте CSS классы в `css/style.css`
3. Добавьте JavaScript функциональность в `js/main.js` (если нужно)

---

## 📱 Тестирование

### Локальное тестирование

```bash
npm run dev
```

Открывается на `http://localhost:3000`

### На разных устройствах

1. Откройте DevTools (F12)
2. Кликните на иконку телефона (Toggle device toolbar)
3. Выберите устройство: iPhone, iPad, Android

### Производительность

1. DevTools > Lighthouse
2. Выберите "Mobile"
3. Кликните "Analyze page load"
4. Целевой Score: 90+

### Валидация HTML

1. Откройте https://validator.w3.org/
2. Скопируйте HTML из `index.html`
3. Вставьте в валидатор
4. Исправьте ошибки (если есть)

---

## 🌐 Развертывание

### Вариант 1: GitHub Pages (БЕСПЛАТНО ⭐)

**Самый простой способ для быстрого старта**

```bash
# 1. Инициализируйте репозиторий
git init

# 2. Добавьте все файлы
git add .

# 3. Первый коммит
git commit -m "Initial commit: Landing page"

# 4. Добавьте remote (замените username)
git remote add origin https://github.com/yourusername/remont-landing.git

# 5. Отправьте на GitHub
git push -u origin main
```

**В GitHub:**

1. Откройте репозиторий
2. Settings > Pages
3. Source: Select branch "main"
4. Сайт будет доступен по адресу: `https://yourusername.github.io/remont-landing`

### Вариант 2: Netlify (БЕСПЛАТНО + функции)

**Лучше для продакшна с лучшей производительностью**

1. Перейдите на https://www.netlify.com/
2. Кликните "Sign up" > "GitHub"
3. Авторизуйтесь в GitHub
4. Выберите репозиторий `remont-landing`
5. Build settings:
   - **Build command:** `npm run build` (или оставьте пусто)
   - **Publish directory:** `./` (корень)
6. Кликните "Deploy"
7. Сайт будет доступен по адресу типа: `https://your-site-name.netlify.app`

### Вариант 3: Собственный хостинг (Платный)

Рекомендуемые хостинги:
- [Beget](https://beget.com/) — русский хостинг
- [REG.RU](https://www.reg.ru/) — русский хостинг
- [Hosting.com](https://www.hosting.com/) — международный
- [Bluehost](https://www.bluehost.com/) — популярный

**Этапы:**

1. Зарегистрируйте домен (например, remontkvartir.ru)
2. Арендуйте хостинг
3. Подключите домен к хостингу
4. Скопируйте все файлы на сервер через FTP:
   - **Программа:** FileZilla, WinSCP
   - **Папка:** `/public_html/` или `/www/`
5. Включите HTTPS (обычно бесплатно с Let's Encrypt)
6. Сайт будет доступен по вашему домену

---

## 🔍 SEO Оптимизация

### Шаг 1: Создайте Sitemap

Создайте файл `sitemap.xml` в корне проекта:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://yoursite.ru/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```

### Шаг 2: Создайте robots.txt

Создайте файл `robots.txt` в корне проекта:

```
User-agent: *
Allow: /
Sitemap: https://yoursite.ru/sitemap.xml
```

### Шаг 3: Регистрация в поисковиках

1. **Google Search Console:**
   - Перейдите https://search.google.com/search-console
   - Добавьте сайт
   - Загрузите `sitemap.xml`

2. **Яндекс.Вебмастер:**
   - Перейдите https://webmaster.yandex.ru/
   - Добавьте сайт
   - Загрузите `sitemap.xml`

### Шаг 4: Проверьте Meta-теги

В `index.html` в теге `<head>` убедитесь, что есть:

```html
<meta name="description" content="Профессиональный ремонт квартир...">
<meta name="keywords" content="ремонт квартир, отделка, ремонт Москва">
<meta property="og:title" content="Ремонт квартир под ключ">
<meta property="og:description" content="От косметики до люкса...">
<meta property="og:image" content="https://yoursite.ru/images/og-image.jpg">
```

---

## 🆘 Решение проблем

### Проблема: Изображения не загружаются

**Решение:**
1. Проверьте пути в HTML (должны быть как `/images/...`)
2. Убедитесь, что файлы существуют в папке
3. Перезагрузите страницу (Ctrl+F5)
4. Проверьте консоль браузера (F12 > Console)

### Проблема: Кнопки не открывают Telegram

**Решение:**
1. Проверьте Telegram username в `main.js`
2. Убедитесь, что ссылка корректная: `https://t.me/yourusername`
3. Проверьте консоль браузера на ошибки

### Проблема: Медленная загрузка страницы

**Решение:**
1. Оптимизируйте изображения (используйте TinyPNG, ImageOptim)
2. Сжимайте JPEG до 100-200KB за файл
3. Используйте WebP формат
4. Включите ленивую загрузку (lazy loading)

### Проблема: Скрипты не работают

**Решение:**
1. Откройте DevTools (F12)
2. Перейдите в Console
3. Посмотрите ошибки
4. Проверьте пути к файлам JS в HTML

---

## 📊 Мониторинг и аналитика

### Что отслеживать

1. **Google Analytics:**
   - Количество посетителей
   - Время на странице
   - Bounce rate
   - Конверсии (клики на Telegram)

2. **Яндекс.Метрика:**
   - Географию посетителей
   - Источники трафика
   - Действия пользователей

### Оптимизация по данным

Если bounce rate высокий:
- Улучшите заголовок и подзаголовок
- Добавьте больше социального доказательства
- Улучшите скорость загрузки

Если мало конверсий:
- Сделайте CTA кнопки более видимыми
- Добавьте больше отзывов
- Упростите процесс контакта

---

## 🔄 Обновления и поддержка

### Регулярные обновления

```bash
# Обновление зависимостей
npm update

# Проверка уязвимостей
npm audit
```

### Резервное копирование

```bash
# Создайте резервную копию перед обновлениями
git tag backup-2024-01-16
git push origin backup-2024-01-16
```

---

## 🎯 Чеклист перед запуском

- [ ] Все контакты обновлены (телефон, email, Telegram)
- [ ] Все изображения загружены и оптимизированы
- [ ] Отзывы актуальны и подтверждены
- [ ] Фото команды профессионального качества
- [ ] Google Analytics настроена и работает
- [ ] Яндекс.Метрика настроена и работает
- [ ] Все ссылки на Telegram рабочие
- [ ] Lighthouse Score 90+ на мобильных
- [ ] Протестировано на iPhone и Android
- [ ] HTTPS включен (зелёный замок в браузере)
- [ ] Sitemap.xml загружена в Google Search Console
- [ ] robots.txt создан и корректен
- [ ] Open Graph теги заполнены для соц. сетей
- [ ] Тестовые заявки отправляются в Telegram

---

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте консоль браузера:** F12 > Console
2. **Посмотрите документацию:** Читайте README.md и CONFIG.md
3. **Погуглите ошибку:** Скопируйте текст ошибки в Google
4. **Посетите Stack Overflow:** https://stackoverflow.com/

---

**Удачи с запуском! 🚀**

Если всё прошло успешно, поделитесь проектом на GitHub и Twitter! 📣
